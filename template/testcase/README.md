# Test case field definition
```json
[{
    "case": "The description of this test case",
    "enable": "Enable or disable this test case",
    "jpool": [{
        "name": "The name of one JSON",
        "data": "The data of one JSON"
    }],
    "template": "The JSON template",
    "schema": "The JSON schema which will match against the result from exectuing the template"
}]
```
